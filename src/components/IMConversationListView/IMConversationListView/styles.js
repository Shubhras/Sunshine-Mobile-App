import { StyleSheet } from 'react-native';
import { STANDARD_FLEX } from '../../../constants/Constants';

// Exporting style
export default StyleSheet.create({
  mainWrapper: {
    flex: STANDARD_FLEX,
  },
  backgroundImage: {
    flex: 1,
    alignItems: 'center',
  },
});
